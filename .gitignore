# Synced GitHub repositories (43GB+ of nested clones) — never version-control this
/GitHub/

# Local sync state / analytics data
.sync-state/

# Design docs / WIP from other work — keep this repo tooling-only
/docs/

# Your personal repo skip-list may contain private repo names — keep it local.
# (.syncignore.example is tracked as documentation.)
/.syncignore

# macOS noise
.DS_Store
